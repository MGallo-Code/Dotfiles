# check-no-secrets allowlist (INV-1 escape hatch).
# One reviewed substring or path per line. A finding whose file path OR matched line
# contains any entry here is suppressed. Lines starting with # are comments.
# Add an entry ONLY after confirming the match is content, not a live credential,
# and note why. Keep this list short; a bloated allowlist is a disabled gate.
#
# (empty - the tree is clean as of 2026-06-17; only ssh/config.template is tracked)
